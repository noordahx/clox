```
README.md
clox - A tree-walk interpreter for the Lox language.



DEBUG TOOLS/COMMANDS:

compile with -g flag for debugging symbols

DEBUG WITH LLDB:
$ lldb ./clox

TO CHECK MEMORY LEAKS:
$ valgrind --leak-check=full --show-leak-kinds=all --track-origins=yes ./clox


```
